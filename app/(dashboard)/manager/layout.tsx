"use client";

import React from 'react'

const SalesLayout = ({ children }: Readonly<{ children: React.ReactNode }>) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default SalesLayout